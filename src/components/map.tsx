import MapComponent from './map_component';

const Geotagging = () => {

    return (
        <div className="geotagging-container">
            <MapComponent />
        </div >
    );
};

export default Geotagging;
